// A function to calculate the area of a rectangle

function CalculateAreaOfRectangle(length, width) {
  const area = length * width;
  return area;
}
  
console.log("The area of the rectangle is: " + CalculateAreaOfRectangle(5, 10));